📝 To-Do List Viewer

This is a simple beginner project I built while learning JavaScript and working with APIs.
It fetches a list of to-do items from a public API and displays them in a clean table using Bootstrap.

You can search tasks by their title and see how many to-dos are currently visible.

✨ What this project does

* Loads to-do data from a public API using JavaScript fetch

* Displays the data in a Bootstrap-styled table

* Lets you search to-dos by title

* Updates the count based on search results

🛠️ Built With

* HTML

* JavaScript

* Bootstrap 4

🌐 API Used

The project uses the following free API:

https://jsonplaceholder.typicode.com/todos/

Important note :

This is a to-do list viewer, not a full to-do application.
You can view and search tasks, but you can’t add, edit, or delete them yet.
